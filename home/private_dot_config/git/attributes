# ~/.config/git/attributes
# Global attributes to improve diffs and line endings.

* text=auto eol=lf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# Treat images and archives as binary (no text diffs)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.pdf  binary
*.zip  binary
*.7z   binary
*.gz   binary
*.tar  binary

# Lockfiles: skip textual diffs to reduce noise
package-lock.json -diff
yarn.lock        -diff
pnpm-lock.yaml   -diff

# Better language-aware diffs with linguist (if difftools leverage it)
*.md  diff=markdown
*.rs  diff=rust
*.go  diff=golang
*.py  diff=python
*.ts  diff=javascript
*.tsx diff=javascript
*.js  diff=javascript
*.jsx diff=javascript
*.sql diff=sql
