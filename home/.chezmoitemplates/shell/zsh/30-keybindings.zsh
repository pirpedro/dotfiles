#   ---------------------------
#   KEYBINDINGS
#   ---------------------------
# exemplo: vi-mode
# bindkey -v

# Autosuggestions
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=8'  # ajuste ao tema

# History substring search (setas ↑/↓)
bindkey '^[[A' history-substring-search-up
bindkey '^[[B' history-substring-search-down
